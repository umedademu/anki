# 逆一問一答学習アプリ 仕様書

## 概要

このアプリは、最初に用語を示し、その用語に関する複数の質問を順番に確認する「逆一問一答」形式の学習アプリです。
個別の知識を思い出したあとに統合説明を読むことで、用語に対する理解と記憶を深めることを目的とします。

## 提供形態

- パソコンやスマートフォンのブラウザで利用するWebアプリ
- Vercel上での公開を想定
- 学習内容はCloudflare R2から読み込む構成を想定

## 現在の実装

- 世界史の100用語を学習できる画面
- 回答前の手掛かりを避けるため、問題画面には用語のみを表示
- 用語ごとに複数の質問を順番に表示
- 「答えを見る」を押すまで答えを隠す
- 全質問の終了後に統合用の問い、統合説明、重要語句を表示
- 次の用語へ進み、50用語ごとに次の分割データを読み込む
- パソコンとスマートフォンの画面幅に対応

## 学習の流れ

1. 学習する用語を表示する
2. その用語に関する質問を1つ表示する
3. 利用者が考えたあと、「答えを見る」で答えを表示する
4. 次の質問へ進み、登録されている質問がなくなるまで繰り返す
5. 全質問の終了後、個別の知識をつないだ統合説明を表示する
6. 次の用語へ進む

質問数は3問などの固定値にしません。CSVに`question_1`、`question_2`のように連番で用意された質問を、存在する分だけすべて表示します。用語ごとに質問数が異なる場合にも対応します。

## 学習データ

### 元データ

- 保存場所：`data/source/world-history/world_history_skeleton_100.csv`
- 現在の件数：100用語、400問
- 1行を1用語として扱う
- 時期・地域・単元などの情報はデータとして保持するが、回答前の問題画面には表示しない
- `question_番号`と`answer_番号`を組として質問を登録する
- `q番号_axis`は「定義」「原因」など質問の観点として表示する
- `integrated_question`と`total_explanation`を統合部分に使用する

### Web表示用データ

- `npm run build:data`でCSVからJSONを生成する
- 科目一覧、科目情報、用語データに分ける
- 用語データは50件ごとに分割し、必要な分だけ読み込む
- 現在は`public/data`へ生成し、Cloudflare R2への登録前でも動作確認できる
- Cloudflare R2の公開URLを設定したあとは、WebアプリがR2上の同じ構成を直接読み込む

## 想定する利用者

- 資格試験や学校の勉強をしている人
- 語学や専門知識を身につけたい人
- 覚えた内容を定期的に復習したい人

## 今後追加する機能（案）

- 学習する科目・単元・時代の選択
- 用語の順番変更と無作為出題
- 学習状況や進み具合の確認
- 復習が必要な問題の表示
- 日本史や英語などの科目追加

## 今後決めること

- 学習記録の保存方法
- 復習する時期の決め方
- 利用者情報の管理方法
- 学習内容を追加・修正する管理画面の要否
